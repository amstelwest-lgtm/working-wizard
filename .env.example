# Supabase — required for all environments
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=<anon-key>
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=<anon-key>

# Session signing secret (TanStack Start SSR)
SESSION_SECRET=<random-64-char-string>

# ── Anthropic Claude (primary AI for owner board) ─────────────────────────────
# Used by:
#   - Industry Pulse (server fn)          → Claude Sonnet 4.6
#   - Financial statement PDF extract     → Claude Sonnet 4.6
#   - Bank statement P&L drafter          → Claude Sonnet 4.6
#   - Bank → cash forecast (Phases 1–3)   → Claude Sonnet 4.6
#   - Advisory drafter (server fn)        → Claude Sonnet 4.6
# Also set the same key as a Supabase secret for edge functions (Ask AI + extract):
#   supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_API_KEY=sk-ant-...
# Optional override (default: claude-sonnet-4-6)
# CLAUDE_MODEL=claude-sonnet-4-6

# ── Optional / legacy AI keys ─────────────────────────────────────────────────
# LOVABLE_API_KEY=<lovable-gateway-key>   # email/webhook gateway only
# OPENAI_API_KEY=sk-...                   # unused by owner-board AI paths

# ── Ask AI / extract edge functions (Supabase secrets, NOT .env) ──────────────
#   supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
#   supabase secrets set CLAUDE_MODEL=claude-sonnet-4-6   # optional
#   supabase secrets set ALLOWED_ORIGINS=https://your-app.vercel.app
#
# SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY are injected
# automatically by the Supabase runtime.

# ── QuickBooks Online (optional) ──────────────────────────────────────────────
# QBO_CLIENT_ID=
# QBO_CLIENT_SECRET=
# QBO_REDIRECT_URI=https://your-app.vercel.app/api/qbo/callback
# QBO_ENVIRONMENT=sandbox

# ── Email (Resend — usually Supabase secrets for edge fns) ────────────────────
# RESEND_API_KEY=re_...
# RESEND_FROM_EMAIL=Milōn <noreply@yourdomain.com>
# SITE_URL=https://your-app.vercel.app
# VITE_APP_URL=https://your-app.vercel.app

# ── Google Sign-In (Supabase Auth) ────────────────────────────────────────────
# No extra app env vars. Enable the Google provider in the Supabase dashboard
# (Authentication → Providers → Google) and add these Redirect URLs:
#   http://localhost:5000/auth/callback
#   https://your-app.vercel.app/auth/callback
# Also add the same callback under Authentication → URL Configuration.

# ── Platform owner ops console (/ops) ─────────────────────────────────────────
# Comma-separated emails allowed into Milōn Forge (defaults to amstel.west@gmail.com)
# MILON_OWNER_EMAILS=amstel.west@gmail.com
# Landing secret passphrase (default: MilonOpsForge) — change in production
# MILON_OPS_PASSPHRASE=change-me-in-prod
